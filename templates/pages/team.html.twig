{% extends 'base.html.twig' %}

{% block title %}Teams{% endblock %}

{% block main %}
  <br/>
  <div class="container" align="center">
    <h2>Display Teams</h2>
    {% if teams %}
      <table class="table table-hover">
        <thead>
          <tr>
            <th scope="col">Team</th>
          </tr>
        </thead>
        <tbody>
          {% for team in teams %}
            <tr class="table-secondary">
              <td>{{team.teamName}}</td>
            </tr>
          {% endfor %}
        </tbody>
      </table>
    {% else %}
      <p>
        No teams to display</p>
      {% endif %}
    </div>

  {% endblock %}
